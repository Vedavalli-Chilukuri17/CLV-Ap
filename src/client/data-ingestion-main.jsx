import React from 'react';
import ReactDOM from 'react-dom/client';
import DataIngestionApp from './data-ingestion-app.jsx';

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <DataIngestionApp />
  </React.StrictMode>
);